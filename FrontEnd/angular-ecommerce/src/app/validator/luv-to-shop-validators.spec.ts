import { LuvToShopValidators } from './luv-to-shop-validators';

describe('LuvToShopValidators', () => {
  it('should create an instance', () => {
    expect(new LuvToShopValidators()).toBeTruthy();
  });
});
